import styles from './MenuMobileButtonContainer.module.scss';

export default function MenuMobileButtonContainer({ children }) {
  return <div className={styles.MenuMobileButtonContainer}>{children}</div>;
}
